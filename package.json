{
  "name": "miniprogram-i18n",
  "version": "1.0.0",
  "description": "The internationalizational (i18n) library for wechat miniprogram",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:compile && npm run test:gulp:wxml && npm run test:gulp:locales && npm run test:core",
    "test:compile": "jest --config=scripts/jest.compile.config.js --verbose",
    "test:core": "jest --config=scripts/jest.core.config.js --verbose",
    "test:gulp:wxml": "jest --config=scripts/jest.gulp.wxml.config.js --verbose",
    "test:gulp:locales": "jest --config=scripts/jest.gulp.locales.config.js --verbose",
    "postinstall": "lerna bootstrap",
    "lint": "tslint -c tslint.json 'packages/**/*.ts'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ele828/miniprogram-i18n.git"
  },
  "author": "wechat-miniprogram",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ele828/miniprogram-i18n/issues"
  },
  "homepage": "https://github.com/ele828/miniprogram-i18n#readme",
  "devDependencies": {
    "@types/gulp": "^4.0.5",
    "@types/jest": "^23.3.10",
    "jest": "^23.6.0",
    "lerna": "^3.6.0",
    "rollup": "^0.68.0",
    "rollup-plugin-replace": "^2.1.0",
    "rollup-plugin-typescript": "^1.0.0",
    "tslib": "^1.9.3",
    "typescript": "^3.2.2"
  },
  "dependencies": {
    "ts-jest": "^23.10.5"
  }
}
