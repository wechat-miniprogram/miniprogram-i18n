{
  "name": "@miniprogram-i18n/core",
  "version": "1.0.0-rc.9",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf dist && npm run build:js && npm run build:wxs && cp package.json dist",
    "build:js": "rollup -c rollup.config.js",
    "build:wxs": "rollup -c rollup.wxs.config.js"
  },
  "author": "",
  "license": "MIT",
  "miniprogram": "lib",
  "dependencies": {
    "format-message-interpret": "^6.2.0",
    "message-format": "^6.2.0",
    "shallowequal": "^1.1.0"
  },
  "devDependencies": {
    "@types/shallowequal": "^1.1.0"
  }
}
